import Hummingbird

extension RegistryController {

    func publishPackageRelease(request: Request, context: some RequestContext) -> PublishPackageReleaseResponse? {
        return nil
    }

    struct PublishPackageReleaseResponse: ResponseEncodable {
    }

}
